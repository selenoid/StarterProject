
<Page loaded="loadUpTestAppView">
  <StackLayout>
    <Button tap="{{getNewNumber}}" horizontalAlignment="center"  text="Get New Number" cssClass="actionButton" />

    <StackLayout orientation="horizontal" horizontalAlignment="center" verticalAlignment="center" minHeight="50">
      <Label text="{{'Nu Integer: ' + nuInteger}}" cssClass="infoLabel" horizontalAlignment="left" cssClass="infoLabel"/>
      <Label text="{{'Old Integer: ' + oldInteger}}" cssClass="infoLabel" horizontalAlignment="right" cssClass="infoLabel"/>
    </StackLayout>

  </StackLayout>
</Page>